<%= link_to gravatar_for(current_user, size: 50), current_user %>
<div class="profile_home">
<h1 class="text-center"><%= current_user.username %></h1>
<span><%= link_to "view my profile",current_user %></span><br>
<span class="text-success"><%= pluralize(current_user.microposts.count,"micropost") %></span>
</div>
